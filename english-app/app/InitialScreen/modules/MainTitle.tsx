import styled from "styled-components";

const StyledMainTitle = styled.h1`
  font-size  : 2.5rem;
  font-weight: bold;

`;

const MainTitle = () => {
    return(
        <StyledMainTitle>Welcome to Kera App!!</StyledMainTitle>
    )
}

export default MainTitle;